<!--index.wxml-->
<view class="container">
      <list-content wx:for="{{ items }}" d="{{ item }}" wx:key="{{ index }}"></list-content>
      <view class='loading' hidden="{{ isLoading }}">正在加载...</view>    
</view>


